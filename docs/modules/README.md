# Module Documentation

Create one document per cohesive code area. Each document must contain: purpose, exact file map, public interfaces, control/data flow, dependencies, invariants, failure modes, and tests.
